# PAMIĘĆ — MusicBox (notatki asystenta)

> Komenda przywracania: `/przywróć pamięć`
> Po ponownym uruchomieniu PC wpisz tę komendę, a wczytam ten plik i kontynuuję pracę.

## ⚠️ NAJWAŻNIEJSZE — zacznij od tego po przywróceniu pamięci
- **0.5.9 ZBUDOWANY (APK w mobile/bin), CZEKA NA TEST NA XIAOMI.** Naprawa long-pressa playlist (tap działał, przytrzymanie nie).
  - Podejrzane root causes: (a) `_tile_touch_move` anulowało timer przy ruchu ≥ dp(10) — realny palec drga i kasował long-press; (b) `_show_playlist_menu` = pierwszy MDDialog w apce, bez try/except → wyjątek ginął do logcata → user widział „nic".
  - Zmiany: `_tile_touch_move` anuluje tylko przy ruchu > dp(30); `_tile_long_fired` strzela gdy `tile.collide_point(*tile._down_touch.pos)` (zamiast dystansu dp(10)); diagnostyka `_debug_log` (tile: down/LONG fired/BAIL powodów + menu: MDDialog FAILED); fallback menu → `_flash_delete_fallback` = Snackbar z przyciskiem „Usuń" (bez nested MDDialog); `_log_env` → 0.5.9.
  - Po teście: jeśli nadal nie działa → podpiąć Xiaomi → pull `musicbox_debug.log` (wskaże dokładny moment bail/fail).
- **Stan teraz: 0.5.9 w mobile/bin; na Xiaomi jest 0.5.8 (tap działa, long-press nie).**

## Styl użytkownika (komunikacja) — WAŻNE, utrzymuj ten ton
- Użytkownik pisze po polsku, KRÓTKO, na luzie, często bez znaków interpunkcyjnych, z literówkami.
- Zwroty: „lecisz", „lecisz braciak", „działa git", „na tip top", „nwm", „spoko", „dobra", „zaradzisz na to jakoś", „XDDD", „wgraj", „przywróć pamięć".
- Często jedna wiadomość = jeden update/bug. Konkretnie, bez lania wody.
- OD CIEBIE: odpowiadaj tak samo — krótko, konkretnie, w tym samym tonie (nieformalnie); struktura (nagłówki/-) jeśli trzeba; ✓/emoji oszczędnie; nie pisz elaboratów. Po `/przywróć pamięć` zachowaj ten sam styl i zwracaj się tak samo.
- Wersjonowanie: wersje po kolei (0.4.x), a **1.0.0 dopiero gdy użytkownik powie, że jest „perfect"**. Nie wydawać 1.0.0 samodzielnie.

## Projekt
- Aplikacja odtwarzacz muzyki **MusicBox** (tytuł APK: MusicBox, package: org.musicbox.musicbox).
- Lokalizacja: `C:\Users\ThinkPad\Desktop\Music player`
- Moduły:
  - `core/` — czysta logika (playlist, storage, stats, library, tags, downloader, cover, summary_pillow) — testowalna bez Kivy. Karty podsumowań: `core/summary_pillow.py` (Pillow, wspólny PC+mobile; stary Qt `summary_image.py` usunięty).
  - `mobile/musicbox/` — aplikacja Android (Kivy 2.3.0 + KivyMD 1.2.0): `app.py`, `audio.py`, `android_io.py`, `controller.py`.
  - `mobile/buildozer.spec` — konfiguracja buildu APK (WSL + buildozer).
  - `ui/` + reszta na Pulpicie — wersja desktopowa.
- Zakres v1 (mobile): odtwarzanie lokalnych utworów, playlisty (import .m3u), biblioteka, statystyki. YouTube pominięty (yt-dlp/ffmpeg niedostępne na Androidzie).

## Aktualny stan (2026-08-05)
- **0.5.8 ZAINSTALOWANY na XIAOMI (boot OK, 2 playlisty) — fix TAP/PRZYTRZYMANIE (usunięty `touch.button`).** 0.5.7 miał martwe kafelki playlist (root cause: SDL2MotionEvent button=None, pułapka #16).
- **0.5.7 ZAINSTALOWANY na XIAOMI (boot OK: perms True/True, 2 playlisty).** Zawiera: 0.5.6 (przytrzymanie→Usuń) + fixy P1/P2 z analizy person (patrz log). Desktop exe przebudowany (swobodny wybór lokacji + „Importuj .m3u" w sidebarze + fixy core).
- **Wersja APK: 0.5.4 — zainstalowany i zweryfikowany na XIAOMI 2506BPN68G (Android 16, adb serial UKYLPVRW5DCUFUSC).** Boot 0.5.4 OK: api_level=36, android=True, build=0.16s, perms [READ_MEDIA_AUDIO, POST_NOTIFICATIONS]=[True,True], bez crasha. **Import .m3u przeszedł („młody say10" → 11 utworów → playlista → refresh_home).** all_files_access=False (brak „Wszystkich plików"), ale import działa bez bramki (od 0.3.8). Instalacja przez adb nie była blokowana przez MIUI (Success). Moto (ZY22HXF268) aktualnie odłączone — 0.5.4 czeka tam na odblokowanie+test. Poprawki review person P1–P3 (patrz log).**
- **0.4.9:** `app.py build()`: usunięte `root.padding = (0, dp(status_bar_height), 0, 0)` — z `fullscreen=0` okno jest już POD status barem, padding podwajał odsunięcie → pusty pas u góry. Nagłówki ekranów (nazwa płyty + przycisk powrotu) i home (nazwa wyżej, przyciski, kafelki) podjeżdżają.
- **Wcześniejsze w 0.4.8:** szary pasek zniknięty (clearcolor po load_string + canvas.before na root); tytuł/okładka w powiadomieniu aktualizują się (CMD_META: titles+covers, has_covers; fix NameError CMD_META).
- **adb:** `C:\Users\ThinkPad\AppData\Local\Temp\opencode\platform-tools\adb.exe`. Budowa: sync_to_wsl.sh → prep_service.sh → build_fg.sh; APK z WSL → **Copy-Item \\wsl.localhost\Ubuntu\root\...**. Moto: tile y=900, track y=460, NEXT x=756 y=2250 (tapy bywają kapryśne).

## Następne kroki / TODO
- [ ] **TEST 0.5.9 na Xiaomi:** przytrzymaj kafelek playlisty → menu → „Usuń" → potwierdzenie (playlista + plik .m3u z MusicBox/ kasują się). Jeśli tap OK ale long-press nadal „nic" → pull musicbox_debug.log (logi tile:/menu:).
- [ ] **TEST 0.5.7 na Xiaomi:** przytrzymaj kafelek playlisty → „Usuń" → potwierdzenie (playlista + plik .m3u z MusicBox/ kasują się, bez odrodzenia po skanie/restarcie).
- [ ] **Desktop exe (przebudowany):** test „Importuj .m3u" z sidebaru (wybór z dowolnego folderu), zapamiętywanie ostatniego katalogu w dialogach, „Wygeneruj obraz" dla „Rok 2026" (QThread, UI nie zastyga).
- [ ] **Test podsumowań na Xiaomi:** Statystyki → „Rok 2026" → „Karta + udostępnij" (potwierdzić share MediaStore na MIUI).
- [ ] **Odblokuj Moto → otwórz MusicBox (0.5.7)** → Statystyki → Rok 2026 → Karta + udostępnij (test share).
- [ ] **Backlog z analizy person (P3 + decyzje, patrz sekcja „Persony i backlog"):** m.in. rok grudzień→listopad w podsumowaniu, profil.json na współdzielonej pamięci, dodanie analizy desktop ui/.
- [ ] **1.0.0 dopiero gdy user powie „perfect".**
- **WAŻNE (desktop):** po zmianach w źródle desktop PRZEBUDOWAĆ exe (`python -m PyInstaller MusicBox.spec --noconfirm`, exe zamknięty).
- **WAŻNE (sync WSL):** `sync_to_wsl.sh` kopiuje TERAZ też `core/` (był bug: ImportError display_artist — core nie był synchronizowany!). Po zmianach w core/ → sync.
- **WAŻNE (workflow APK):** po buildzie APK TYLKO do `mobile/bin/`; na Pulpicie NIE gromadzić wersji.

## Ważne decyzje i pułapki (nauki z RESUME_BUILD.md)
1. Kopia źródeł w WSL `/root/musicbox-build/mobile/` jest NIEZALEŻNA od Windowsa — po edycjach trzeba ją zsynchronizować (komenda sync_to_wsl.sh).
2. Wersja w `buildozer.spec` musi być podbijana przy każdej przebudowie (unikatowa nazwa APK).
3. WSL Ubuntu 26.04 (user: root). Buildozer 1.6.0, p4a.branch=develop, p4a.update=False.
4. PEP 668 → `PIP_BREAK_SYSTEM_PACKAGES=1` wymagane w WSL.
5. `nohup ... &` przez `wsl ... bash -c` → proces zabijany gdy wsl wraca. Trwały build: Start-Process wsl.exe na przedzie (build_fg.sh).
6. Kivy 2.3.0 NIE wspiera Pythona 3.14 → requirements `hostpython3==3.11.9,python3==3.11.9`.
7. NDK r28c (clang 18) traktuje `-Wincompatible-function-pointer-types` jako ERROR przy Kivy 2.3.0 → patch CFLAGS w `pythonforandroid/archs.py` (w .buildozer — znika przy p4a.update=True/clean).
8. WSL RAM zwiększony do 6GB; vmIdleTimeout=-1 w `.wslconfig` (VM nie gaśnie).
9. Desktop: plyer filechooser nie działał na Androidzie → natywny `pick_m3u()` przez jnius (ACTION_GET_CONTENT + on_activity_result).
10. `Playlist.load_m3u` rozwiązuje ścieżki względne względem folderu pliku .m3u — dlatego przy materializacji do temp względne ścieżki zawodzą.
11. **KV NIE jest walidowane przez py_compile** — błędy OptionProperty wykrywane dopiero przy Builder.load_string (crash przy starcie). Opcje: `shorten_from`: left/center/right; `valign`: top/middle/bottom; `halign`: left/center/right/justify. Przed buildem sprawdzać nowe wartości KV pod kątem OptionProperty (Kivy label.py ~linia 962).
12. **jnius InputStream:** Java InputStream ma `read()`, `read(byte[])`, `read(byte[],int,int)` — NIE ma `read(int)`. Czytanie strumienia: bajt-po-bajcie `stream.read()` (int, -1=EOF) — 100% pewne; `stream.read(bytearray)` bywa zawodne; `bytes(stream.read())` złe (read() = 1 bajt int → bytes(int) = NUL).
13. **Detekcja Androida:** NIE polegać na `import android` na górze modułu (może cicho paść → appka w trybie desktop!). Używać env: `ANDROID_ARGUMENT`/`ANDROID_APP_PATH`/`P4A_BOOTSTRAP` (p4a ustawia je w PythonActivity.java:135,157).
14. **Logi dla usera:** `getFilesDir()` = /data/user/0/... (prywatny, niewidoczny); do logów widocznych dla usera używać `getExternalFilesDir(None)` = /storage/emulated/0/Android/data/<pkg>/files.
15. **jnius Java obiekty:** NIE używać `str(obiekt_jnius)` do pobrania stringa — zwraca Python repr (`<android.net.Uri at 0x...>`)! Używać metody Java `.toString()`. To był ROOT CAUSE importu .m3u (0.1.3 → 0.2.2).
16. **SDL2MotionEvent ma `button=None`:** na Androidzie dotyk (palec) NIE ustawia `touch.button` (motionevent.py:244, window_sdl2.py SDL2MotionEvent). Warunek `touch.button == "left"` jest ZAWSZE False → handler dotyku martwy. NIE sprawdzać `touch.button` na Androidzie; wzorzec: `TrackRow.on_touch_down/up` (collide_point + _down_pos + odległość < dp(10)). To był ROOT CAUSE „klikanie i przytrzymanie nie działa" (0.5.7).
17. **subprocess z stdout/stderr=PIPE + pętla `poll()` bez czytania pipe = DEADLOCK:** jeśli dziecko pisze dużo do stderr (ffmpeg progres), bufor pipe (~4KB) się zapycha → dziecko blokuje na write → `poll()` wraca None → proces „wisi" do timeoutu/killa. Czytać pipe w WĄTKACH (`threading.Thread` z `stream.read(64KB)`) przez cały czas pracy, nie dopiero `communicate()` po zakończeniu. To był ROOT CAUSE „pobieranie utknęło na 1. kawałku" (desktop, 2026-08-08).
18. **ytmusicapi `YTMusic.search()` NIE ma timeoutu** (requests bez timeout) → zawieszone żądanie = wątek wisi w NIESKOŃCZONOŚĆ. Fix: `socket.setdefaulttimeout(30)` na starcie wątku workera. (desktop, 2026-08-08).

## Persony i backlog (analiza z 2026-08-05)
Wybrane persony (do code review / debugowania), ścieżka: `C:\Users\ThinkPad\Desktop\AI persony\`:
- **Marek** `dev/persona-dev-python.md` → `core/` (typy/kontrakty/walidacja)
- **Ola** `dev/persona-dev-android.md` → `mobile/` (stany UI, wątki, UX)
- **Janek** `dev/persona-dev-desktop.md` → `ui/` (ścieżki/dystrybucja/QThread)
- **Igor** `qa/persona-qa-analyst-python.md` → root cause, regresja
- **Natalia** `security/persona-security-appsec-native.md` → uprawnienia/bezpieczeństwo
- **Michał** `architect/persona-architect-mvvm.md` (NOWA 2026-08-08) → architektura MVVM, uniwersalna (desktop+mobile); blueprint: Model=dane+DTO (core/), View=usługi/agregaty (services/), ViewModel=DTO→prezentacja+komendy (viewmodel/), View=cienki (ui/ + mobile KV). Zalecenie Miszy (kolega usera): MVVM jako wariant serwerowego MVC.
Nierelewantne: web-backend, web-frontend, database-designer/optimizer, qa-data/web, appsec-web.

ZNAPANE I NAPRAWIONE (P1/P2) w 0.5.6/0.5.7 + exe (patrz log):
- mobile: `_scanning` przyklejone (finally); wyścig na `library.playlists` (skan na kopii); startup merge zamiast podmiany; przeciek wątków `_send_playlist_meta` (sentinels None); import .m3u na wątku worker; `_on_media_state` fałszywe statystyki (matched>=0 + reset `_last_state_path` przy ended → liczy repeat-one); plyer callback przez Clock; profil import cleanup; `allow_backup=False`.
- core: stats `_load` walidacja+clean (crash na uszkodzonym JSON), `save` atomowe, `format_listening(None)` guard, `min_year` filtr wzorca; downloader `parse_m3u` (plain .m3u bez EXTINF + cp1250), per-video `.tmp`, `%%` w szablonie yt-dlp, `start_new_session` (killpg nie ubija appki), sanitize_name cap 180; storage `get/set_last_dir`, `save_settings/load_settings` atomowe+walidacja, `save_playlist` zwraca None przy błędzie, `delete_playlist` try/except; cover cache 64 + multi-APIC; library.load_m3u pusty → None; summary_pillow None-guard.
- desktop: dialogi pamiętają folder (never cwd/exe), przycisk „Importuj .m3u" w sidebarze.

BACKLOG (do decyzji, NIE ruszane):
- [ ] **Rok podsumowania = grudzień→listopad** (`stats._build_year_summary`) — semantyka „Roku 2026" może być zamierzona (Spotify-style); zmiana wymaga zgody usera.
- [ ] **`profil.json` na współdzielonej pamięci** (Natalia P2) — celowa funkcja sync MusicBox/; kompromis prywatność vs feature.
- [ ] **`downloader._kill_proc` killpg** — już zabezpieczone `start_new_session`, ale tylko win32/Android, gałąź POSIX martwa.
- [ ] **Analiza desktop `ui/` per persona Janek** — pełna, zalecona (na razie pokryta tylko przez file-location + testy).
- [ ] **P3 mobile:** tożsamość touch w kafelkach (multi-touch), ikona play/pauza przy starcie, cache okładek (temp+rename), `_on_media_state` nie liczy przy braku profilu.
- [ ] **Per-track progres yt-dlp w UI** (desktop) — parsowanie progresu z stderr do paska (obecnie postęp tylko per-utwór); `track_timeout` roboczo rozwiązuje „utknięcie".

### Test 0.5.9 desktop (download) na maszynie usera
- [ ] Uruchom `dist\MusicBox.exe` (tytuł okna pokaże „MusicBox v0.5.9-desktop (build 2026-08-08)" — weryfikacja, który exe).
- [ ] Pobierz playlistę CSV → pierwszy utwór nie może „utknąć": ffmpeg kończy w kilka sekund, budżet 300s/utwór, błąd/wznowienie przy problemach. Log: `%APPDATA%\...\musicbox_dl.log` (wersje narzędzi + per-track ok/err).

## Komendy
- Testy (Windows, bez kivy): `python -m pytest tests/ -q --ignore=tests/test_main_window.py` (91 passed)
- Testy kontrolera: `python -m pytest tests/test_mobile_controller.py tests/test_playlist.py -q`
- Sprawdzenie składni: `python -m compileall -q core ui mobile/musicbox`
- Sync do WSL: skrypt `C:\Users\ThinkPad\AppData\Local\Temp\opencode\sync_to_wsl.sh` (wzór), uruchamiany: `wsl -d Ubuntu -u root -- bash /mnt/c/Users/ThinkPad/AppData/Local/Temp/opencode/sync_to_wsl.sh`
- Build APK: pełna procedura w `RESUME_BUILD.md` (AKTUALNY KROK: 9 — test importu .m3u na telefonie).
- Przywrócenie pamięci: wpisz `/przywróć pamięć`.

## Log zmian
- 2026-08-08: macOS — BUILD PRZEZ GITHUB ACTIONS („odpal i używaj" jak exe). User: „nie ma czegoś takiego jak w Windowsie że odpala i jest git?". ODPOWIEDŹ: tak — przez GitHub Actions (chmura macOS), bo .app MUSI powstać na macOS (PyInstaller nie cross-kompiluje z Windows). ZMIANY: nowy **`.github/workflows/build-macos.yml`** (runs-on macos-latest; instaluje Python universal2 python.org 3.12.8 pkg przez sudo installer; pip install -r requirements.txt -r requirements-dev.txt; `bash build_macos.sh`; ditto → zip MusicBox.app; upload-artifact MusicBox-macOS; workflow_dispatch + push paths core/ui/main.py/MusicBox_macos.spec/build_macos.sh); **`build_macos.sh`** +tryb `MUSICBOX_TOOLS_REQUIRED=1` (CI) — twarda weryfikacja kompletności bin/macos/{arm64,x86_64}/{yt-dlp,ffmpeg,deno}, exit 1 gdy brak (apka bez narzędzi bezużyteczna); nowy **`MACOS_BUILD.md`** — instrukcja: (1) push na GitHub, (2) Actions → Build macOS App → Run workflow, (3) pobierz artifact MusicBox-macOS → MusicBox.app, (4) prawy klik Otwórz / xattr quarantine, (5) gotowe; + opcja B lokalny build; README link do MACOS_BUILD.md. WERYFIKACJA: bash -n OK (WSL). NAUKA: skrypt wspólny dla lokalnego i CI builda — tryb strict przez env (MUSICBOX_TOOLS_REQUIRED) zamiast osobnych ścieżek.
- 2026-08-08: WERSJA macOS (przygotowanie, build lokalny na Macu kolegi). User: kolega ma MacBooka M1, chce Apple Silicon + Intel, `.app`, pełna parzystość z exe. REALITY: PyInstaller nie cross-kompiluje dla macOS — plik .app musi powstać NA Macu (build_macos.sh). PyInstaller 6.21 wspiera `--target-architecture universal2` (sprawdzone w źródle: {'x86_64','arm64','universal2'}). ZMIANY (Janek/Marek): `ui/main_window.py _icon_paths` → na darwin preferuje `assets/icon.icns`; `core/downloader.py` `_mac_arch()` (platform.machine → arm64/x86_64) + `_candidate_dirs` na macOS dokłada `bin/macos/<arch>` przed `bin/` (frozen i nie); nowy **`MusicBox_macos.spec`** (Analysis target_arch='universal2', EXE windowed upx=False, BUNDLE MusicBox.app, bundle_identifier org.musicbox.musicbox, icon assets/icon.icns); nowy **`build_macos.sh`** (pobiera narzędzia: yt-dlp_macos universal → bin/macos/{arm64,x86_64}/yt-dlp, deno-aarch64/x86_64-apple-darwin.zip → deno, evermeet ffmpeg arm64+x86_64 zip; najlepsza-effort — przy nieudanym pobraniu ostrzega i appka używa PATH/Homebrew; generuje icon.icns przez sips+iconutil; pip install -r requirements.txt -r requirements-dev.txt; pyinstaller MusicBox_macos.spec → dist/MusicBox.app); README sekcja „Budowa wersji macOS" + Gatekeeper (xattr -dr com.apple.quarantine / prawy klik Otwórz). WERYFIKACJA (Windows): compileall OK, spec AST OK, 156 passed (find_tool mac-guarded — Windows bez zmian). NAUKA: universal2 .app + arch-specyficzne narzędzia wybierane przez platform.machine() (bin/macos/<arch>); nie można zbudować macOS z Windowsa.
- 2026-08-08: DESKTOP — SPRZĄTANIE `.tmp` + TOOLTIP TRYBU. User: „pobieram playlistę a w folderze nie ma .m3u i zostaje pusty .tmp". DIAGNOZA: brak .m3u przy trybie „Pobierz jako: Album" to ZAMIERZONE (user wybrał opcję „folder albumu bez .m3u"); pusty `.tmp` to bug (podfolder `<video_id>` usuwany po sukcesie, ale rodzic `.tmp` zostawał). DECYZJA usera: BEZ auto-przełączania trybu (user chce pełną kontrolę). ZMIANY (Janek): `ui/download_worker.py` koniec `run()` → `shutil.rmtree(out_root/.tmp, ignore_errors=True)` (czyszczenie pustego .tmp i resztek po przerwanych utworach; `import shutil`); `ui/download_view.py` tooltip „Pobierz jako": „Playlista — folder z .m3u. Album — folder albumu z tagami (TALB/TRCK/TCON), bez .m3u". TESTY (Igor): nowy `tests/test_download_worker.py` — worker z fake tools/download_track (tracks=) → po run: `Test.m3u` istnieje, `.tmp` usunięty. WERYFIKACJA: 156 passed. EXE przebudowany (dist\MusicBox.exe 08.08 19:42).
- 2026-08-08: DESKTOP — CREDS SPOTIFY DO OSOBNEGO PLIKU (koniec kasowania przy starcie). User: „nie uzupełnia automatycznie id" (2. raz). DIAGNOZA: `settings.json` znowu z pustym `spotify` (choć `download.source=spotify` zapisany); biegnący stary MusicBox (z bugiem zawsze-nadpisywania) nadpisywał `settings.json` swoim stanem w pamięci — bez creds → kasował prefill przed startem nowego exe. ROOT CAUSE ARCHITEKTONICZNY: creds mieszkały w `settings.json`, który jest nadpisywany przez wiele setters — wystarczył bieg z pustym stanem, by je zetrzeć. FIX (Marek): `core/storage.py` — `get/set_spotify_credentials` przeniesione do OSOBNEGO pliku **`spotify_credentials.json`** (w data dir, atomowy zapis tmp+replace); `get_spotify_credentials` fallback do starego klucza `settings["spotify"]` (backward-compat); ŻADEN zapis `settings.json` nie może już ruszyć creds. TESTY (Igor): +test_spotify_credentials_survive_other_settings_writes (set_download_settings/set_last_dir nie ruszają creds), +test_spotify_credentials_fallback_legacy_key. WERYFIKACJA: 155 passed. PREFILL: `storage.set_spotify_credentials(76337a…, 4df82d…)` → `%APPDATA%\MusicBox\spotify_credentials.json` potwierdzone. EXE przebudowany (dist\MusicBox.exe 08.08 18:56). NAUKA: dane, które mają przetrwać zapisy innych ustawień, trzymać w OSOBNYM pliku, nie we wspólnym settings.json.
- 2026-08-08: DESKTOP — TAG TPE1 = JEDEN ARTYSTA (iTunes nie robi osobnego albumu przy dwóch artystach). User: „wpisuj tylko jednego artystę bo iTunes robi wtedy osobny album na utwory na których jest dwóch artystów". DECYZJA (opcja 1, konserwatywna): cięcie po `,` + `feat./ft./featuring`; nazwy zespołów z ` & ` („Simon & Garfunkel", „Earth, Wind & Fire") zostają jednym artystą (guard: pierwszy separator to przecinek ORAZ tekst ma „ & " → nie ciąć). ZMIANY (Marek): `core/downloader.py` nowy `single_artist(name)` + `_ARTIST_SPLIT_RE`; `_build_id3_tags` → `TPE1 = single_artist(track["artists"])`; `track["artists"]` (pełny) ZOSTAJE dla search (`search_candidates` query) i nazwy pliku (`_track_base`) — nie pogarsza trafień. TESTY (Igor): `single_artist("A, B")=="A"`, feat/ft/featuring, „Far East Movement, The Cataracs, DEV"→„Far East Movement", „Simon & Garfunkel"/„Earth, Wind & Fire" nienaruszone, `_build_id3_tags` TPE1==["A"] dla "A, B". WERYFIKACJA: 153 passed. EXE przebudowany (dist\MusicBox.exe 08.08 18:47).
- 2026-08-08: DESKTOP — FIX: CREDS SPOTIFY NIE WYCZYSZCZAJĄ SIĘ PRZY STARCIE. User: „dalej prosi o wpisanie id, nie uzupełniło się samo". DIAGNOZA: `%APPDATA%\MusicBox\settings.json` miał `download.source=spotify` + `download.mode=album` (nowy exe działa), ale `spotify.client_id/secret` PUSTE — pomimo prefillu. ROOT CAUSE: `_persist_spotify_creds` zapisywał ZAWSZE to co w polach; przy starcie `_source_changed` robi `setText(client_id)` → odpala zapis z JESZCZE PUSTYM secretem → plik dostawał `{client_id, client_secret:""}` (wyścig przy starcie kasował creds). FIX: `_persist_spotify_creds` nie nadpisuje pustymi — `cid = pole or istniejace`, `sec = pole or istniejace`, zapis tylko gdy cokolwiek jest. PREFILL ponownie: `storage.set_spotify_credentials(76337a…, 4df82d…)` → potwierdzone w settings.json. TESTY: `test_persist_does_not_wipe_creds_with_empty_fields` (puste pola → creds zostają). WERYFIKACJA: 151 passed. EXE przebudowany (dist\MusicBox.exe 08.08 18:31). NAUKA: persist przy zmianie pola NIE może nadpisywać istniejących danych pustymi stringami — scalać z poprzednim stanem.
- 2026-08-08: DESKTOP — ZAPAMIĘTYWANIE CREDS SPOTIFY + ŹRÓDŁA. User: „zapamiętuj client id i secret żeby nie wpisywać co odpalenie". DIAGNOZA: persistencja creds JUŻ była w kodzie (textChanged→settings.json, ładowanie przy przełączeniu na „Link Spotify"), ale settings.json miał puste creds (user wklejał creds w czacie, nie do apki); źródło (CSV/Spotify) NIE było zapamiętywane (start = zawsze CSV). ZMIANY: **Marek** `core/storage.py` +klucz `source` ("csv"/"spotify") w download settings (default "csv", walidacja); **Janek** `ui/download_view.py` `_source_changed` ZAPISUJE wybrane źródło + zawsze ładuje creds (nie tylko przy przełączaniu); przy starcie ładowana zapamiętana opcja źródła (`findData(saved["source"])` + `_source_changed()` po inicjalizacji `_tracks`). PREFILL jednorazowo: `storage.set_spotify_credentials(76337a…, 4df82d…)` zapisane do `%APPDATA%\MusicBox\settings.json` (user nie musi wpisywać). TESTY: storage roundtrip `source`; `test_download_view.py` +test_source_is_remembered (zmiana źródła persistuje; nowa instancja widoku wczytuje źródło + creds). WERYFIKACJA: 150 passed. EXE przebudowany (dist\MusicBox.exe 08.08 18:18).
- 2026-08-08: DESKTOP — AUTO-ALBUM FIX (przycisk „Pobierz wszystko" nie działał). User: „klikam pobierz wszystko i nie pobiera". DIAGNOZA: log `%APPDATA%\MusicBox\musicbox_dl.log` kończył się na 16:31 (działało: Europe 12/12, Whitesnake 11/11) — po nowym exe ZERO wpisów → worker NIGDY nie startował → `_start` wracało na bramce „Album wymagany" (dodanej dla iPoda): gdy źródło nie ma albumu (link playlista ze Spotify / CSV bez kolumny „Album Name") pole Album puste → przycisk WYŁĄCZONY, klik nic nie robi, bez komunikatu. DECYZJA usera (opcja A): auto-uzupełnienie — gdy źródło nie ma albumu, pole „Album" wypełnia się nazwą źródła/playlisty → przycisk od razu aktywny; user może edytować; WYCZYŚCENIE pola → znowu wymusza ręczne (bramka zostaje). ZMIANA: `ui/download_view.py _apply_tracks` — `album_edit.setText(name)` gdy `not _album_from_csv` + placeholder „Album (edytuj — wzięty z nazwy źródła)…" + status „album wzięty z nazwy źródła — możesz edytować"; usunięty zbędny prefill album-mode (pierwsza Album Name — zastąpiony przez name). TESTY: nowy `tests/test_download_view.py` (QApplication, wzorzec jak test_main_window): CSV bez albumu → auto-fill nazwą pliku + przycisk aktywny; CSV z albumem → pole nietknięte + aktywny; wyczyszczenie auto-filla → blokada. WERYFIKACJA: 149 passed (117+29+3). EXE przebudowany (dist\MusicBox.exe 08.08 17:53). NAUKA: bramki walidacji blokujące całą akcję muszą mieć widoczny stan (disabled button + status); auto-fill z sensownym domyślnym > twardy wymóg.
- 2026-08-08: DESKTOP — POBRANIE ZE SPOTIFY (CSV ALBO LINK). User (za radą Miszy, personą Michała): pobieranie z linku Spotify — albo plik CSV, albo link. Ustawienia: user założył darmową appkę Spotify Developer (client_id 76337a..., secret ...7e9a). TESTOWANE: token client_credentials OK; utwór → Web API `/v1/tracks` OK; album → `/v1/albums` OK (track_number/disc_number); **playlista → Web API tracks = 403 z client_credentials** (Spotify wymaga usera) → obejście: **embed page** `open.spotify.com/embed/playlist/{id}` → `__NEXT_DATA__` → `trackList` (title/subtitle/uri/duration(ms)); NAUKA: `__NEXT_DATA__` structure `props.pageProps.state.data.entity`, `trackList` to LISTA (nie dict). ZMIANY (personami): **Michał/Marek** nowy `core/spotify.py` (czyste urllib, zero deps): `spotify_token` (client_credentials), `parse_spotify_url` (spotify:/open.spotify.com/intl-pl/embed), `resolve_spotify_link` → (nazwa, [Track]) jak parse_csv, `_resolve_album` z paginacją (>50 utworów); **Janek** `ui/download_view.py` przełącznik „Źródło: Plik CSV / Link Spotify" + pole linku + pola Client ID/Secret (persist w settings.json przez `storage.get/set_spotify_credentials`, secret Password echo), `SpotifyLoadWorker` (QThread) + `_apply_tracks` (wspólny dla CSV i Spotify), `DownloadWorker` rozszerzony o `tracks=`/`playlist_name=` (skoro tracki już zresolvowane), album mode zachowuje `track_no` ze Spotify (`augment_album_tracks` tylko uzupełnia brakujące). WERYFIKACJA: pytest 117 + Qt 29 (nowe test_spotify.py: parse_spotify_url warianty, token error, resolve track/album/playlist z fake HTTP; augment_album_tracks_preserves_spotify_numbers; storage creds roundtrip); UI offscreen (przełącznik widoczność/creds); SMOKE end-to-end: album „Whenever You Need Somebody" → `01 - Rick Astley - Never Gonna Give You Up.mp3`, TRCK `1/10`, TCON `Pop`, TALB OK. EXE przebudowany (dist\MusicBox.exe 08.08 17:22). UWAGA bezpieczeństwo: client_secret tylko w settings.json, nie w logach; user wklejał creds w czacie → może obrócić w panelu Spotify.
- 2026-08-08: DESKTOP — ALBUM ZAWSZE W TAGACH (wymuszenie ręczne, exe 0.6.0-desktop). User (iTunes po chińsku): plik bez pola Album gubi się przy sync na iPoda. ROOT CAUSE: `_tag_mp3` dodawało TALB tylko gdy `album` niepuste; tryb playlista NIE ustawiał albumu gdy CSV nie miał kolumny „Album Name" → pliki bez tagu. DECYZJA usera: gdy albumu nie da się odczytać z CSV → WYMUŚ ręczne wpisanie (opcja 1: wpisany uzupełnia TYLKO brakujące, realne albumy z CSV zostają). ZMIANY (persony): **Marek** `core/downloader.py` nowa `ensure_album(tracks, fallback)` (czysta, album z utworu albo fallback, brak fallbacku = bez zmian); **Janek** `ui/download_view.py` pole „Album" + `_album_from_csv` (any album w CSV) → gdy brak → placeholder „Album (wymagany — brak w CSV)", przycisk „Pobierz wszystko" zablokowany do wpisania (`_update_download_enabled`), status informuje; `ui/download_worker.py` playlista → `ensure_album(tracks, options['album'])`, album → album_name = pierwsza „Album Name" z CSV albo wpisana, potem `ensure_album(tracks, album_name)`; **Igor** testy `ensure_album` (fill tylko brakujące / pusty fallback bez zmian). WERYFIKACJA: pytest 110 passed + Qt 29; UI offscreen: bez albumu→blokada+placeholder, wpisanie→odblokowanie, z albumem w CSV→od razu OK. EXE przebudowany (dist\MusicBox.exe 08.08 16:20, PYZ core.downloader OK).
- 2026-08-08: DESKTOP — TRYB ALBUM (z CSV) + GATUNEK + clipping paska (exe 0.6.0-desktop). User (iPod): potrzebuje albumu, nie playlisty — iPod nie czyta .m3u, potrzebuje tagów (album + nr utworu). SKAN CSVMusic.exe (PYZ): miał format m4a/mp3 + jakość + okładkę (`cover_bytes`/`cover_size`, `covr`, APIC "Cover") + link albumu/playlisty, ale NIE tagował TRCK/TPOS/TCON (gatunku). DECYZJE: album = TEN SAM CSV (kolejność z CSV), tryb Playlista/Album, gatunek jedno pole na całość, folder albumu bez .m3u, okładka = 1. utwór dla wszystkich. ZMIANY: `core/downloader.py` — `_tag_mp3` refaktor → `_build_id3_tags` (+TRCK `NN/total`, +TPOS, +TCON), `_track_base` (prefix `NN - ` gdy track_no>0), `download_track(..., cover_bytes=None)` (okładka wspólna zamiast per-utwór), `augment_album_tracks(tracks, genre)` (track_no 1..N, total=N, disc_no=1, genre), `parse_csv` opcjonalnie czyta "Track Number"/"Disc Number", `_int_or_0`; `core/storage.py` get/set_download_settings +mode ("playlist"/"album") +genre; `ui/download_view.py` — przełącznik "Pobierz jako: Playlista/Album" + pole "Gatunek" + zapamiętywanie (preset quality porównuje teraz tylko bitrate+cover, persist zachowuje mode/genre); `ui/download_worker.py` — tryb album (album_name = pierwsza "Album Name" z CSV, fallback nazwa pliku; okładka z 1. pobranego utworu via `_extract_cover_bytes` reużyta dla reszty; BEZ write_m3u); CLIPPING paska (`ui/player_bar.py`): `ScrollingTitle._reposition` + `setMask(QRegion(widoczny obszar))` — user zgłosił „przewija się, ale nachodzi" na okładkę/sterowanie (root cause: label geometry wychodzi poza widżet). WERYFIKACJA: pytest 108 passed + Qt 29 (nowe: _build_id3_tags TRCK 3/12+TPOS+TCON, _track_base, augment_album_tracks, parse_csv track_no, download_track_uses_provided_cover, storage mode/genre roundtrip); SMOKE album end-to-end: `01/02 - Rick Astley - *.mp3`, TRCK `1/2`+`2/2`, TCON `Pop`, TALB OK. EXE przebudowany (dist\MusicBox.exe 08.08 15:45, PYZ core.downloader OK). NAUKA: mutagen TCON/TRCK/TPOS to TextFrames — `.text` zwraca listę ["3/12"].
- 2026-08-08: 0.6.1 MOBILE — MARQUEE FIX (przewijanie tytułu/autora). User: „nazwa dalej nie ma animacji przesuwania". ROOT CAUSE: `ScrollView(do_scroll_x=False)` w Kivy wyłącza też PROGRAMOWE `scroll_x` (setter to no-op gdy do_scroll_x=False) → label poszerzany ale nie scrollował (przycięty). FIX: `MarqueeLabel` przepisany BEZ ScrollView — ręczne rysowanie tekstu w `canvas` z clippingiem przez StencilPush/StencilUse/StencilPop, timer 0.03s/1px, pauza 1.5s, wyśrodkowanie gdy się mieści (CoreLabel do pomiaru szerokości). WALIDACJA przez hostpython3 w WSL: long→scrolling=True, fits/empty→False, KV_OK. BUILD APK 0.6.1 SUKCES (52 897 547 B), w mobile/bin, ZAINSTALOWANY na Xiaomi przez adb (Success, versionName 0.6.1, boot OK perms True/True, 2 playlisty). CZEKA NA TEST przewijania na Xiaomi.
- 2026-08-08: 0.6.0 DESKTOP + MOBILE — ustawienia pobierania + autor pod tytułem + przewijanie. (A) USTAWENIA POBIERANIA (desktop, persony Marek/Janek/Igor): `core/downloader.py` `download_track(..., options={"mp3_bitrate":"320k","cover_size":600})` + `normalize_download_options` (walidacja bitrate `^\d{2,4}k$`, cover_size int) + czysta `_ffmpeg_cmd(ffmpeg,raw,final,bitrate)`; `core/storage.py` `get/set_download_settings()` (settings.json, dozwolone bitrates 128k/192k/320k, cover 300/600, default 320k/600); `ui/download_view.py` ComboBox „Jakość pobierania": Standard 320k / 192k / 128k / **Legacy iPod (192k + 300px okładka)** + tooltip, zapamiętywanie (`_load_quality_preset`/`_persist_quality`); `download_worker` przekazuje `options`. WERYFIKACJA: smoke 192k → mp3 192kbps 5.16MB (vs 320k 8.6MB), cover_size przekazany. (B) PASEK DESKTOP (Janek): `ui/player_bar.py` drugi `ScrollingTitle` autora (`subtitleLabel`, h=16) pod tytułem, `set_track(title, artist, pixmap)`; `main_window._on_track_changed` → `display_artist(track.path,"")`; marquee zwolniony (SCROLL_STEP_PX 2→1 ≈33px/s). (C) PASEK MOBILE (Ola/Marek): nowy `mobile/musicbox/marquee.py` `MarqueeLabel` (BoxLayout + ScrollView do_scroll=False + MDLabel; `CoreLabel` do pomiaru szerokości; Clock tick 0.03s/1px, pauza 1.5s na końcu; mieści się → halign center, za długie → marquee) zamiast `shorten` w `<PlayerBar>` (track_label sp(15) + artist_label sp(12) pod spodem); wersja buildozer 0.6.0 + `_log_env` 0.6.0. WALIDACJA mobile przez hostpython3 w WSL (Kivy 2.3.0/KivyMD 1.2.0): KV_OK (marquee import + PlayerBar rule). BUILD APK 0.6.0 SUKCES (52 897 723 B) w mobile/bin, pakowany kod OK. TESTY: 102 passed (nowe: _ffmpeg_cmd, normalize_download_options, download_track_passes_cover_size, storage download_settings×3) + 29 Qt passed. EXE przebudowany (dist\MusicBox.exe 08.08 05:14, tytuł okna „MusicBox v0.6.0-desktop (build 2026-08-08)"). UWAGA: exe rebuild wymaga zamknięcia działającego MusicBox.exe (PermissionError). APK 0.6.0 CZEKA NA TEST na Xiaomi (przewijanie tytułu/autora), exe do testu pobierania (ComboBox jakości).
- 2026-08-08: DESKTOP — ROOT CAUSE „utknięcia pobierania na 1. kawałku" = DEADLOCK PIPE w `core/downloader.py _run`. User: „podczas pobierania playlisty utknął na pierwszym kawałku bez końca". Wersje exe sprawdzone: dist\MusicBox.exe (05.08 22:54) był AKTUALNY względem kodu; yt-dlp w bundlu = 2026.07.04 (NAJNOWSZY na 08.08, działa, ekstrakcja OK); to nie był stary exe. Diagnoza przez reprodukcję (persony Igor/Janek/Marek/Natalia): webm pobierał się w 4.6s, ale `ffmpeg` w `_run` WISIŁ 594s (rc=1 po zabiciu); bezpośrednio ffmpeg kończył w 6.5s. PRZYCZYNA: `_run` trzymał stdout/stderr jako PIPES i NIE czytał ich w pętli `while` (czytał dopiero przez `communicate()` PO zakończeniu) → ffmpeg pisze progres do stderr → bufor pipe (~4KB) się zapychał → ffmpeg blokował na write → deadlock do timeoutu (600s). To był „utknięty pierwszy kawałek". FIX: `_run` odpala wątki `_drain` (czytanie 64KB) na stdout/stderr PRZEZ CAŁY czas pracy procesu; `proc.poll()` po killu dla returncode. DOBUDOWA (anty-hang, persony): `socket.setdefaulttimeout(30)` na starcie `download_worker.run()` (ytmusicapi 1.12.1 search NIE ma timeoutu → bez tego nieskończony hang na szukaniu); `search_candidates(ytmusic, track, n=3)` zamiast `search_best` (fallback po kolei videoId, `ytsearch1` ostatnia deska); `download_track(..., track_timeout=300)` — budżet/utwór liczony przed każdym `_run` (koniec 600s×3 prób = 30 min/utwór); `core/version.py` (APP_VERSION 0.5.9-desktop + BUILD_DATE) → tytuł okna „MusicBox v… (build …)" (wcześniej desktop NIE pokazywał wersji — stąd prośba usera „sprawdź wersje exe"); log `musicbox_dl.log` w data dir z wersjami narzędzi przy starcie pobierania. WERYFIKACJA: pytest 96 passed (nowe: test_run_timeout_kills_proc, test_run_drains_pipes_no_deadlock, test_search_candidates_*, test_download_track_stops_after_deadline); smoke test end-to-end (Rick Astley) 12.7s OK (mp3 8.2MB) — wcześniej 594s+ timeout; exe przebudowany (dist\MusicBox.exe 08.08 04:29, PYZ zawiera core.downloader). NAUKA/PUŁAPKA (#17): subprocess.Popen z stdout/stderr=PIPE + pętla poll() bez czytania pipe = DEADLOCK przy dziecku piszącym dużo do stderr (ffmpeg progres) — czytać pipe w wątkach przez cały czas.
- 2026-08-08: BUILD 0.5.9 SUKCES — naprawa long-pressa playlist. Zmiany (app.py): `_tile_touch_move` anuluje timer tylko przy ruchu > dp(30) (wcześniej dp(10) — drgający palec kasował long-press); `_tile_long_fired` strzela gdy palec wciąż w kafelku (`collide_point` zamiast dystansu dp(10)); diagnostyka `_debug_log` we wszystkich krokach (tile: down / long BAIL z powodem / LONG fired; menu: MDDialog otwarty / FAILED repr); `_show_playlist_menu` w try/except → fallback `_flash_delete_fallback` = Snackbar z przyciskiem „Usuń" (bez nested MDDialog, bo MDDialog może być root cause); `_log_env` → 0.5.9. compileall OK, pytest 91 passed. sync→prep_service→build OK, pakowany kod zweryfikowany (grep: dp(30)×2, LONG fired, MDDialog FAILED). APK w mobile/bin (52 891 839 B). CZEKA NA TEST na Xiaomi; jeśli nie działa → pull musicbox_debug.log.
- 2026-08-05: 0.5.8 TAP I PRZYTRZYMANIE ZNÓW DZIAŁAJĄ. User: „klikanie ani przytrzymanie nie działa". ROOT CAUSE: w `_tile_touch_down` dodałem `and touch.button == "left"` — na Androidzie SDL2MotionEvent ma `button=None` (Kivy 2.3.0 motionevent.py:244), więc warunek ZAWSZE False → `_down_pos`/timer nie startowały → tap i long-press martwe. FIX: usunięty warunek button (wzorzec jak działający TrackRow). NAUKA (pułapka #16): NIE sprawdzać `touch.button` na Androidzie. BUILD 0.5.8 SUKCES, APK w mobile/bin, instalacja na Xiaomi.
- 2026-08-05: 0.5.7 FIXY P1/P2 Z ANALIZY PERSON (Marek/Ola/Janek/Igor/Natalia). Mobile: `_scanning` reset w `finally` (`_scan_playlists_async`), skan na kopii listy (koniec wyścigu na `library.playlists`), `_startup_applied` MERGE zamiast podmiany listy, przeciek wątków `_send_playlist_meta` (po `q.join()` sentinele None ×4), import .m3u w wątku worker (`_import_worker`/`_import_applied` — koniec ANR przy MediaStore), `_on_media_state` liczy odsłuch tylko gdy ścieżka w playliście (koniec fałszywych statystyk) + reset `_last_state_path` przy ended (repeat-one liczy się), plyer callback przez Clock (desktop-mode), `_on_profile_selected` cleanup, `android.allow_backup=False`. Core: stats `_load` walidacja dict + clean (crash na uszkodzonym stats.json), `save` atomowe (tmp+replace), `format_listening(None)` guard, `min_year` filtr `_MONTH_KEY_RE`; downloader `parse_m3u` (plain .m3u bez EXTINF + wielo-kodowanie), per-video `.tmp` (koniec rmvaj race), `%%` w szablonie yt-dlp (tytuł z `%`), `start_new_session` w `_run` (killpg nie ubija appki), `sanitize_name` cap 180; storage `save_settings/load_settings` atomowe+walidacja, `save_playlist` zwraca None przy błędzie, `delete_playlist` try/except; cover cache 64 + multi-APIC; library.load_m3u pusty → None; summary_pillow None-guard. Testy: 91 passed. BUILD 0.5.7 SUKCES, APK w mobile/bin, zainstalowany na Xiaomi (boot OK).
- 2026-08-05: 0.5.6 PRZYTRZYMANIE → USUŃ. Kafelek playlisty na home: własne `on_touch_down/up/move` (`_tile_touch_down/_tile_touch_up/_tile_touch_move/_tile_long_fired`), timer 0.5s + sprawdzanie `touch.pos` (scroll anuluje), tap → open_playlist, przytrzymanie → MDDialog z „Usuń"/„Anuluj", potwierdzenie „Usunąć playlistę?" (Usuń/Anuluj) → `_delete_playlist` → `_remove_playlist_by_name` (stop audio gdy grana) + `storage.delete_source_file` (kasuje .m3u z MusicBox/ po nazwie) + `storage.delete_playlist` (wewn. kopia) → koniec odrodzenia przy skanie/restarcie. `core/storage.py`: nowe `delete_source_file(name, external_dir)` (testowalne). Testy: +3 w test_storage (11 w pliku), BUILD 0.5.6 SUKCES, APK w mobile/bin, zainstalowany na Xiaomi (boot OK).
- 2026-08-05: DESKTOP SWOBODNY WYBÓR LOKACJI. ROOT CAUSE: PyInstaller exe → cwd=folder exe → wszystkie QFileDialog bez katalogu startowego otwierały się w folderze exe, bez pamięci ostatniego miejsca. FIX: `core/storage.py` `get_last_dir(key,default)`/`set_last_dir(key,path)` (settings.json, walidacja istnienia); dialogi desktop (`download_view.py` CSV+folder, `main_window.py` profil eksport/import + obraz statystyk) startują z zapamiętanego folderu albo Documents (nigdy cwd/exe) i zapisują wybór; nowy przycisk „⇧ Importuj .m3u" w sidebarze (`importRequested` → `_on_import_playlist` → QFileDialog *.m3u/*.m3u8 → `_handle_m3u_drop`). Testy: 4 nowe w test_storage. exe przebudowany.
- 2026-08-04: Utworzono ten plik. Poprawki z review (6 punktów, patrz „Aktualny stan"), wersja 0.1.4, sync do WSL, 75 testów OK.
- 2026-08-04: BUILD 0.1.4 SUKCES (przyrostowy, ~2 min). APK w mobile/bin/ i na Pulpicie. Poprawki zweryfikowane w spakowanym kodzie.
- 2026-08-04: NAPRAWA IMPORTU .M3U v2 (root cause: read_bytes 1 bajt + /tmp niezapisywalne + brak uprawnień runtime). BUILD 0.1.5 SUKCES, APK w mobile/bin/ i na Pulpicie, READ_MEDIA_AUDIO dodane.
- 2026-08-04: DOSTĘP DO PAMIĘCI („Wszystkie pliki") + widoczny folder MusicBox/ + kopiowanie .m3u tam. MANAGE_EXTERNAL_STORAGE, settings intent na imporcie. BUILD 0.1.6 SUKCES, APK w mobile/bin/ i na Pulpicie.
- 2026-08-04: FOLDER MusicBox v2 — folder tworzony zawsze (start/import/refresh home), kopia archiwalna .m3u także dla realnych ścieżek, label ze ścieżką na home. BUILD 0.1.7 SUKCES, APK w mobile/bin/ i na Pulpicie.
- 2026-08-04: NAPRAWA CRASHA — `shorten_from: "middle"` (niedozwolone w Kivy) → "center". BUILD 0.1.8 SUKCES, APK w mobile/bin/ i na Pulpicie. Nauka o OptionProperty w KV dodana do pułapek.
- 2026-08-04: AUTO-SKAN playlist (build + show_home) + NAPRAWA IMPORTU (root cause: `stream.read(8192)` — InputStream nie ma read(int) → read_bytes()=b"" → materializacja cicho zawodziła od 0.1.5) + komunikaty + musicbox_debug.log. BUILD 0.1.9 SUKCES, APK w mobile/bin/ i na Pulpicie. NAUKA: Java InputStream ma read(), read(byte[]), read(byte[],int,int) — NIE read(int); jnius: bytearray→byte[] działa, int nie jest konwertowany na byte[].
- 2026-08-04: WERSJA DIAGNOSTYCZNA 0.2.0 — log do storage.get_data_dir() (zawsze zapisywalny), banner startowy, log on_activity_result/picker/resolve/scan, status na ekranie. CZEKA na log od usera (Moto g54 5G: zgoda TAK, picker się otwiera, ale import nic). BUILD 0.2.0 SUKCES, APK w mobile/bin/ i na Pulpicie. NAUKA: app_storage_path() = getFilesDir() = /data/user/0/<pkg>/files — PRYWATNY, user nie zobaczy logu; log musi iść do getExternalFilesDir(None) = /storage/emulated/0/Android/data/<pkg>/files (widoczny).
- 2026-08-04: NAPRAWA DETEKCJI ANDROIDA 0.2.1 — _ANDROID po env (ANDROID_ARGUMENT/ANDROID_APP_PATH/P4A_BOOTSTRAP), nie po import android; _read_stream_all bajt-po-bajcie; widoczny log (getExternalFilesDir/MusicBox); komunikat przy pustej selekcji; status android=True/False na ekranie. BUILD 0.2.1 SUKCES, APK w mobile/bin/ i na Pulpicie.
- 2026-08-04: 0.2.2 + TEST ADB NA XIAOMI 15T PRO — usunięto bramkę „Wszystkie pliki" z importu; logi czytania w skanie; ZWERYFIKOWANO na urządzeniu (logcat): android=True, READ_MEDIA_AUDIO granted, skan MusicBox → "dodano test2" → playlista zapisana. Mechanizm działa. adb w C:\Users\ThinkPad\AppData\Local\Temp\opencode\platform-tools\.
- 2026-08-04: 0.2.3 — ROOT CAUSE IMPORTU: `str(jnius.Uri)` zwraca repr, nie URI → `uri.toString()`. Import przez picker NIGDY nie działał (0.1.3+), skan tak. NAUKA: jnius Java obiekty — do stringa używać .toString(), NIE str(obj).
- 2026-08-04: 0.2.4 — UX: brak auto-grania po otwarciu playlisty; auto-odświeżanie po imporcie (home+skan+refresh). Zainstalowane przez adb.
- 2026-08-04: FINALIZACJA 0.3.0 — czysty folder_label, log pod MUSICBOX_DEBUG, usunięta obietnica profil.json; README.md + .gitignore + git init (commit c27ae16); sprzątanie (do_analizy 2,4GB, build/, __pycache__, .pytest_cache, 15 starych APK). PROJEKT FINALNY.
- 2026-08-04: WYGLĄD 0.3.1 — pełny ekran (Window.clearcolor), okładki MDCard (playlista/biblioteka/home kafelki), statystyki 1:1 (rename/reset/eksport/import + podsumowania miesiąc/rok), auto-sync profilu (profil.json w MusicBox/, import przy starcie). UWAGA: MIUI blokuje adb install → instalacja ręczna lub "Install via USB" w opcjach deweloperskich.
- 2026-08-04: WYMUSZENIE+CACHE 0.3.2 — pełne wymuszenie „Wszystkich plików" (brama Importuj + auto-prompt start + komunikat); przycisk refresh na home + auto-check co 10s; cache: okładki cache-first (nie czyta mp3 gdy okładka w cache), async okładki (wątek+Clock), cache tytułów JSON (path+mtime). NAUKA: przy cache okładek sprawdzać plik cache PRZED extract_cover (inaczej każdy build czyta mp3).
- 2026-08-04: SUWAK+W TLE 0.3.3 — suwak: _scrubbing (seek po puszczeniu); odtwarzanie w tle: KeepAliveService (Java, ten sam proces), onTaskRemoved stopSelf. NAUKA: manifest p4a z AKTYWNYCH szablonów dists/musicbox/templates + bootstrap_builds/sdl2/templates, NIE z _sdl_common; prep_service.sh przed buildem.
- 2026-08-04: NAPRAWA 0.3.4 (3 zgłoszenia usera). ROOT CAUSE tła: SIGABRT w wątku AudioTrack przy zamrożeniu przez MIUI (logcat 11:31:23) — mimo FGS proces pada natywnie. Fix: foregroundServiceType=mediaPlayback + startForeground 3-arg + MediaStyle/MediaSession + wake lock; WAKE_LOCK w spec. WOLNE ŁADOWANIE: tagi w tle (4 workery), okładki przez wspólną kolejkę (2 workery) — koniec blokady UI mutagen.File. SUWAK: defensywnie _updating_slider wokół slider.max (brak seek przy clamp). LOGI: _debug_log zawsze-on (bez MUSICBOX_DEBUG), rotacja 1MB, log providera. NAUKA: MIUI blokuje adb input tap (INJECT_EVENTS denied) — test UI tylko ręcznie. BUILD 0.3.4 SUKCES (~2 min, incremental), APK w mobile/bin/ i na Pulpicie, zainstalowany przez adb.
- 2026-08-04: PRAWDZIWY ROOT CAUSE 0.3.5 — **Kivy używa SDL2 zamiast MediaPlayera**: `kivy/core/audio/audio_android.py` robi `from android import api_version` (na import), a p4a's `android/__init__.pyc` ma TYLKO `co_names=['android._android']` → **HAS api_version: False** → ImportError → provider android pomijany → SDL2. Skutki SDL2: pos=0 (martwy suwak), brak seeka, SIGABRT przy onPause (tło). FIX: własny backend `android.media.MediaPlayer` przez jnius w audio.py (setDataSource/prepare/pause/start/seekTo/getCurrentPosition/getDuration/OnCompletionListener). WOLNOŚĆ: FUSE `stat` ~22ms/op (52 pliki=1.15s w shellu) — usunięto per-utworowe getmtime/isfile z main-threadu (tylko workery), **RecycleView+TrackRow** (lazy) dla playlisty/biblioteki, skan+load playlist w wątku tła (`_startup_async`/`_scan_playlists_async`+lock), cache dirty+flush co 5s, instrumentacja perf (build=0.32s). NAUKA: kopiowanie APK z WSL na Windows — `cp` przez bash bywa cicho zawodne; pewne: **Copy-Item z \\wsl.localhost\Ubuntu\root\...**. BUILD 0.3.5 SUKCES, APK w mobile/bin/ i na Pulpicie, zainstalowany przez adb.
- 2026-08-04: PRZYCISKI W POWIADOMIENIU 0.3.6 — KeepAliveService: 4 akcje MediaStyle (PLAY_PAUSE/NEXT/PREV/STOP, ikony systemowe ic_media_*, PendingIntent.getBroadcast IMMUTABLE+setPackage, setShowActionsInCompactView(0,1,2), setActive(true)), tytuł utworu z Intent extra; android_io.start_playback_service(title); app._setup_media_receiver przez **android.broadcast.BroadcastReceiver** (p4a, callback na wątku handlera → Clock.schedule_once), _on_media_action→toggle_play/play_next/play_prev/stop. NAUKA: sterowanie musi wrócić do Pythona (właściciela MediaPlayera) przez broadcast, bo proces trzyma FGS. Weryfikacja: GenericBroadcastReceiver w classes5.dex, action string w classes4.dex, `media_receiver: ok` w logu. BUILD 0.3.6 SUKCES, APK w mobile/bin/ i na Pulpicie, zainstalowany przez adb.
- 2026-08-04: TYTUŁ+OKŁADKA+PAUZA 0.3.7 — powiadomienie: contentTitle=tytuł (duży), contentText=Odtwarzanie/Wstrzymano, setLargeIcon(okładka z cache), dynamiczna ikona play/pauza (ic_media_play/pause), wake lock TYLKO podczas grania (bateria). app._play_track przekazuje title+cover; toggle_play: pauza→set_playback_paused(True) BEZ stop serwisu (powiadomienie zostaje), wznowienie→set_playback_paused(False). NAUKA: `set_playback_paused` wysyła Intent z samym playing (serwis trzyma title/cover); serwis czyta extras tylko gdy hasExtra. Weryfikacja: setLargeIcon/ic_media_play/EXTRA_PLAYING w classes4.dex. UWAGA: Xiaomi odłączone (ma 0.3.6), Moto g54 5G ZY22HXF268 dostaje 0.3.7. BUILD 0.3.7 SUKCES.
- 2026-08-04: 0.3.8 (3 zgłoszenia usera: Samsung brak promptu, sterowanie tylko w apce, Moto nie działa). SAMSUNG: usunięto wymóg „Wszystkich plików" (import bez bramki, musicbox_dir None bez zgody, open_all_files_settings z 3 fallbackami, przycisk „Nadaj dostęp"). TŁO: register_media_receiver na getApplicationContext() (GenericBroadcastReceiver + PythonJavaClass) — p4a android.broadcast rejestrował na mActivity (ginął przy destrukcji Activity w tle); log media_action. MOTO: uri_to_path zawsze string (koniec TypeError), find_media_paths jedno zapytanie + fallback filesystem. NAUKA: p4a android.broadcast uzywa Activity-context — do tła trzeba app-context. BUILD 0.3.8 SUKCES, APK w mobile/bin/ i na Pulpicie; Xiaomi Download/ (ręczna instalacja), Moto odłączone.
- 2026-08-04: 0.3.9 — Moto nadal: auto-skan 0.3.8 DZIAŁA (playlista dodana, READ_MEDIA_AUDIO granted=true — dlatego brak promptu), ale IMPORT padał: `TypeError('Cannot change graphics instruction outside the main Kivy thread')` — on_activity_result p4a działa na Android UI-thread, a _on_import_selected dotykał grafu Kivy z tego wątku. FIX: pick_m3u marshal on_selected przez Clock.schedule_once (emit). Dodatkowo: request_storage_permissions opóźnione (Clock 1s) + log perms, folder_label pokazuje status dostępu, log refresh_home. NAUKA: callbacki p4a (on_activity_result) są na UI-thread — UI Kivy TYLKO przez Clock.schedule_once. BUILD 0.3.9 SUKCES, APK w mobile/bin/ i na Pulpicie, zainstalowany na Moto (perms True/True, refresh_home: 2 playlist).
- 2026-08-04: 0.4.0 CRASH STEROWANIA — user: apka crashuje przy pierwszym sterowaniu (Moto). Logcat: Fatal signal 6/11/7 (SIGABRT/SIGSEGV/SIGBUS) — backtrace `GenericBroadcastReceiver.onReceive → org.jnius.NativeInvocationHandler.invoke → jnius.so`. ROOT CAUSE: `_Callback(PythonJavaClass)` tworzony inline w register_media_receiver BEZ referencji Pythona → GC zbierał obiekt → Java wywoływała zwolniony obiekt → crash natywny. FIX: `_media_callback_obj = cb` (silna referencja) + try/except w onReceive. WERYFIKACJA na Moto: `am broadcast -a ...PLAY_PAUSE` i `.NEXT` → log `media_action: ...`, apka żyje, NEXT zagrał następny utwór. NAUKA: PythonJavaClass do Javy MUSI mieć żywą referencję (p4a broadcast trzyma self.listener). BUILD 0.4.0 SUKCES, APK w mobile/bin/ i na Pulpicie, zainstalowany na Moto.
- 2026-08-04: 0.4.1→0.4.3 — STEROWANIE W TLE = SERWIS JEST ODTWARZACZEM. User: „zmienia muzykę dopiero po otwarciu apki" → dowód że pętla Kivy zamrożona w tle (proces żyje, audio gra, Clock nie odpala). PRZEBUDOWA: KeepAliveService trzyma MediaPlayer + paths + index + repeat; komendy cmd.* wykonywane w Javie; przyciski powiadomienia = getService; auto-advance po zakończeniu w Javie; broadcasty state.CHANGED/state.POSITION → Python aktualizuje UI przy przodzie. audio.py=_ServiceAudio (klient), desktop=SoundLoader. PUŁAPKI jnius: putExtra(str)→char[], putExtra(int)→short → ClassCastException („Key paths expected String but value was a [C", „Key index expected Integer but value was Short"). FIX: wszystkie extras jako String + parseInt fallback w Javie. WERYFIKACJA na Moto (0.4.3): tick pos=0→28 (pozycja płynie z serwisu), ServiceRecord żyje, po Home ticki stają ale audio_flinger pokazuje aktywny AudioTrack + NotificationRecord actions=4. NAUKA: do sterowania w tle NIE polegać na pętli Kivy — właścicielem MediaPlayera musi być FGS (Java); jnius putExtra wymaga jawnych typów (String). BUILD 0.4.3 SUKCES, APK w mobile/bin/ i na Pulpicie, zainstalowany na Moto.
- 2026-08-04: 0.4.4 WYGLĄD + CRASH FIX — user: okładka w powiadomieniu źle skalowana + szary pasek u góry (status bar) + apka crashuje. FIXY: pillow (normalizacja okładki 512x512, cache covers_v2); set_system_bar_colors (status+nav bar na kolor apki); CRASH: `ClassNotFoundException KeepAliveService` — pillow wymusił regenerację dista, p4a wymazał Javę → `android.add_src = java` w spec (kopiuje Javę co build, odporny na regenerację). NAUKA: wstrzykiwanie Javy do buildozera = `android.add_src`, nie tylko prep_service.sh. WERYFIKACJA na Moto: start bez crasha, ServiceRecord KeepAliveService + cmd.PLAY, audio_flinger gra. BUILD 0.4.4 SUKCES (2 razy), APK w mobile/bin/ i na Pulpicie.
- 2026-08-04: 0.4.5→0.4.8 SZARY PASEK + TYTUŁ/OKŁADKA W POWIADOMIENIU. Szary pas = #121212 (powierzchnia KivyMD) w pustej strefie roota — KivyMD kasował clearcolor; FIX: clearcolor PO load_string + canvas.before #0a0f1e na root. Tytuł/okładka: serwis nie znał meta innych utworów; FIX: CMD_META (titles+covers, tytuły szybko, okładki 4 wątkami, has_covers). PUŁAPKA: NameError CMD_META (zapomniałem stałej w audio.py) — meta cicho padała; diagnostyka ze znacznikami to wykryła. WERYFIKACJA na Moto: meta: wyslano 13, NEXT → powiadomienie android.title=(... – PRAWDA) aktualne, piksele u góry #0A0F1E. NAUKA: przy nowych komendach dodać stałą w audio.py ORAZ Javie; diagnostyka w wątkach (except: pass) ukrywa błędy — logować. BUILD 0.4.8 SUKCES, APK w mobile/bin/ i na Pulpicie, zainstalowany na Moto.
- 2026-08-04: 0.4.9 TREŚĆ DO GÓRY — user: pasek zniknął, całe menu można dać niżej (nazwa płyty, przycisk powrotu, home: nazwa wyżej/przyciski/kafelki). FIX: usunięty `root.padding = (0, dp(status_bar_height),0,0)` w build() — okno i tak pod status barem (fullscreen=0), padding podwajał odsunięcie. BUILD 0.4.9 SUKCES, APK w mobile/bin/ i na Pulpicie, zainstalowany na Moto.
- 2026-08-04: PC — NAPRAWA TOP3 (liczenie odsłuchów). User: na PC nie pokazuje top3 utworów/artystów. Diagnoza: profil "Demo" w stats.json ma listening_seconds=10442 ale play_counts={} — `increment_play` nie odpalał się, bo `trackStarted` silnika zależał od kruchego sprawdzenia `_on_playback_state` (PlayingState + source==_pending_count_source), które cicho nie odpalało. FIX: `core/media.py` `set_track` emituje `trackStarted(path)` BEZPOŚREDNIO (usunięte _pending_count_source/_on_playback_state); resume nie liczy (bez set_track), restore pomijany (_restoring_session). Weryfikacja: set_track → trackStarted (headless Qt OK), 104 tests passed. SYNC: user zrezygnował — ręczne "Wczytaj profil".
- 2026-08-04: SPRZĄTANIE — usunięto 38 starych APK (0.3.0–0.4.8) z mobile/bin i Pulpitu (został tylko 0.4.9 w obu; ~2 GB uwolnione) + 5 katalogów __pycache__/.pytest_cache. ZASADA: po buildzie APK TYLKO do mobile/bin; na Pulpicie nie gromadzić wersji.
- 2026-08-04: PC — TOP3 NIE DZIAŁAŁO, BO USER URUCHAMIAŁ STARY EXE. Fix w źródle (set_track→trackStarted bezpośrednio) był, ale desktop to `dist\MusicBox.exe` (PyInstaller, zbudowany 03.08 PRZED fixem) → stary kod → increment_play nie odpalał (play_counts={} przy listening_seconds>0). FIX: przebudowano exe (`python -m PyInstaller MusicBox.spec --noconfirm`, exe musi być zamknięty). Weryfikacja: headless set_track→trackStarted→increment_play→play_counts/artist_counts OK. NAUKA: desktop = zbudowany exe, po zmianach źródła PRZEBUDOWAĆ.
- 2026-08-04: 0.5.0 SHUFFLE W TLE + KOMUNIKATY + README. Shuffle: play-order (`_play_order`/`_rebuild_play_order` — naturalna albo potasowana, shuffle ON); NEXT/PREV mobile → CMD_NEXT/PREV przez serwis (spójne przód/tło); `CMD_ORDER` w serwisie (zmiana kolejności+meta bez restartu, re-index do obecnej piosenki); `_on_media_state` → naturalny index po ścieżce; meta w kolejności play-order. Komunikaty: `_flash_status` → KivyMD Snackbar (widoczny na każdym ekranie, fallback do empty_label). README finalny (build APK/exe, funkcje serwisu/tła, diagnostyka). BUILD 0.5.0 SUKCES, APK w mobile/bin (bez Pulpitu — zasada), zainstalowany na Moto (start OK, odtwarzanie+meta OK, bez crasha przy shuffle/NEXT).
- 2026-08-04: 0.5.1 WYKONAWCY POD TYTUŁEM. core/tags.py: `display_title`/`display_artist`/`combine_name` (artysta z tagów albo z nazwy pliku przed " - "). Mobile: cache tagów `[mtime,title,artist]`; TrackRow = 2 linie (tytuł góra pełny + wykonawcy dół przygaszeni, height 60→64, RecycleBoxLayout default_size 64); PlayerBar: `artist_label` pod track_label, wys. 120→140; `_update_now_label` → tytuł + wykonawca osobno. Desktop `ui/track_list.py`: kolumna „Wykonawca" + filtr po tytule/wykonawcy; exe przebudowany. **NAUKA/BUG:** `ImportError display_artist` — `sync_to_wsl.sh` nie kopiował `core/` → build używał starego core/tags.py; FIX: sync kopiuje teraz całe `core/`. **NAUKA/CRASH:** `NameError: combine_name` (app.py:932 `_display` cache-hit) — nie było importu; pierwszy start OK, kolejne crash → wtórny abort SDL („attempting to detach"). FIX: import `combine_name` w app.py:39. BUILD 0.5.1 (22:24) SUKCES, APK w mobile/bin, zainstalowany na Moto (2× start OK, bez crasha).
- 2026-08-05: 0.5.4 NA XIAOMI (2506BPN68G, Android 16, serial UKYLPVRW5DCUFUSC). Instalacja adb Success (MIUI NIE zablokowało tym razem). Boot 0.5.4: api_level=36, android=True, build=0.16s, perms True/True, bez crasha. IMPORT .m3u OK („młody say10" → 11 utworów). all_files_access=False ale import działa bez bramki (0.3.8). NAUKA: na Xiaomi adb input keyevent blokowane (INJECT_EVENTS denied) — monkey/am start działają (instrumentacja), keyevent nie.
- 2026-08-05: 0.5.5 USUWANIE PLAYLIST PO SKASOWANIU PLIKU Z FOLDERU. User: „playlisty nie usuwają się po usunięciu pliku z folderu". ROOT CAUSE: skan `_scan_new_playlists` TYLKO dodawał (`if base in known: continue`), nigdy nie czyścił; do tego `storage.save_playlist` dublował każdą playlistę do prywatnego `playlists_dir()` → usunięcie .m3u z MusicBox/ nie kasowało playlisty, a restart i tak ją przywracał z kopii. FIX: `core/storage.py` nowa czysta funkcja `prune_missing(playlists, external_dir)` (testowalna, zwraca kept/removed; gdy external_dir None → bez zmian); app.py: `_scan_new_playlists` zwraca teraz `(new, removed)`, `_apply_scan`/`_remove_playlist_by_name` usuwają playlistę z library + `storage.delete_playlist(name)` (żeby kopia wewnętrzna nie wskrzesiła jej na skanie/restarcie) + stop audio gdy grana; startup też czyści. `_check_refresh` co 10s odpala skan → kasowanie pliku = zniknięcie playlisty w ~10s bez klikania. NAUKA: przy skanach folderów z dwoma kopiami (zewnętrzny MusicBox/ + wewnętrzny playlists_dir) MUSI być usuwana kopia wewnętrzna po wyczyszczeniu zewnętrznego źródła, inaczej wskrzesza. Testy: 85 passed (2 nowe w test_storage.py). BUILD 0.5.5 SUKCES, APK w mobile/bin.
- 2026-08-05: 0.5.4 POPRAWKI PO REVIEW PERSON (P1–P3). P1: `_summary_card_worker` całość w try/except + gwarantowany `_summary_card_done` — wcześniej nieoczekiwany wyjątek (np. save_summary_png) gubił wątek i przycisk wisiał na „Generowanie…" na zawsze. P2: `_summary_card_done` → `_update_summary_button()` (zamiast ręcznego disabled) — spójny stan przy odbudowie ekranu. P3(1) nieużywany import display_artist usunięty; P3(2) `_font` z try/except na load_default(size); P3(3) `_media_store_insert_png` sprząta osierocony wiersz MediaStore (resolver.delete przy błędzie stream); P3(4) desktop `_on_stats_image` przeniesiony do QThread (`_SummaryRenderWorker`, Signal done(bool,path,error), UI nie blokuje przy extract_cover top3). Weryfikacja: 83 passed; QThread headless test OK (render→PNG 55KB→done). BUILD 0.5.4 SUKCES, APK w mobile/bin, exe przebudowany. Instalacja Moto: dumpsys 0.5.4, bundle 0.5.4. NAUKA: telefon z zablokowanym keyguardem → Kivy wisi na „Window: Provider: sdl2" (fps:-1, brak tracebacka) — NIE bug, sprawdzać mWakefulness/mCurrentFocus zanim diagnozować „nie startuje".
- 2026-08-05: 0.5.3 ROCZNE PODSUMOWANIE OD RĘKI + PROFIL DEMO USUNIĘTY. User: „usuń profil demo z apki bo przez to nie moge wygenerować swojego rocznego podsumowania". USTALENIE: demo NIE było w danych apki (mobile „szymon", PC „Szymon" — sprawdzone przez adb run-as i APPDATA). Jedyny artefakt demo = `profil-demo.json` w projekcie → USUNIĘTY. ROOT CAUSE blokady: `core/stats.py` `maybe_create_year_summary` pomijało bieżący rok do 4.12 (`(today.month,today.day) < (12,4)`) → brak „Roku 2026" w spinnerze → nie dało się wygenerować własnej karty rocznej. FIX: bramka usunięta; bieżący rok = podsumowanie „rok-w-dotychczas" tworzone od razu i ODBUDOWYWANE co refresh (świeże odsłuchy), lata przeszłe bez zmian (twórz raz). Usunięte stałe YEAR_SUMMARY_MONTH/DAY. Testy: 83 passed (przebudowane test_year_summary_created_on_dec4 + nowy test_year_summary_created_midyear, usunięty test_no_summary_before_dec4). BUILD 0.5.3 SUKCES, APK w mobile/bin, exe przebudowany, zainstalowany na Moto (start OK, bez crasha).
- 2026-08-04: 0.5.2 PODSUMOWANIA NA MOBILE. User: „mobilka działa jak marzenie ale brakuje generowania podsumowań". **Wspólny renderer `core/summary_pillow.py`** (Pillow 1080x1920, port współrzędnych z Qt 1:1): gradient, pasek akcentu, „CZAS SŁUCHANIA", TOP UTWORY/WYKONAWCY top3 z okładkami (cache-first przez cover_dir md5, fallback numer), stopka; fonty Windows segoeui*/Android Roboto (fallback load_default). Desktop przełączony (ui/main_window.py → summary_pillow), **usunięte core/summary_image.py + test_summary_image.py** (PySide6); nowy tests/test_summary_pillow.py (8 testów, bez Qt, w tym polskie diakrytyki). Mobile: przycisk „Karta + udostępnij" w statystykach (4 stany wg persony Ola: disabled/loading/success/error; generacja w wątku worker, zero I/O na UI). `android_io.save_summary_png` (zapis MusicBox/ lub external_log_dir + **insert MediaStore Downloads** → content:// URI) i `share_file` (ACTION_SEND + FLAG_GRANT_READ_URI_PERMISSION + chooser, bez FileProvidera, zero nowych uprawnień — persony Natalia). NAUKA: MediaStore zamiast FileProvidera do share'u = mniej manifestu/powierzchni ataku; jnius ContentValues.put wymaga String(String) (jak putExtra). BUILD 0.5.2 SUKCES (2×), APK w mobile/bin, zainstalowany na Moto (2× start OK, log 0.5.2, bez crasha). Desktop exe przebudowany (PyInstaller, Pillow w bundlu, start OK).
